performance
    网页性能度量上传和错误上传

2017-06-02
    src/upload-by-image.js：
        通过图片src上传

    src/upload-error.js:
        上传错误

    src/upload-performance.js：
        上传性能（timing && memory && navigation）

    demo/upload-js-css-load-time.html:
        上传外部CSS和JS加载时间
